// This file was autogenerated by the `uniffi-bindgen-gecko-js` crate.
// Trust me, you don't want to mess with it!

import {
  ArrayBufferDataStream,
  FfiConverter,
  FfiConverterArrayBuffer,
  FfiConverterInt8,
  FfiConverterUInt8,
  FfiConverterInt16,
  FfiConverterUInt16,
  FfiConverterInt32,
  FfiConverterUInt32,
  FfiConverterInt64,
  FfiConverterUInt64,
  FfiConverterFloat32,
  FfiConverterFloat64,
  FfiConverterBoolean,
  FfiConverterBytes,
  FfiConverterString,
  UniFFICallbackHandler,
  UniFFICallbackMethodHandler,
  UniFFIError,
  UniFFIInternalError,
  UniFFITypeError,
  constructUniffiObject,
  handleRustResult,
  uniffiObjectPtr,
} from "moz-src:///toolkit/components/uniffi-js/js/UniFFI.sys.mjs";

// Objects intended to be used in the unit tests
export var UnitTestObjs = {
    uniffiObjectPtr,
};
/**
 * Create a new, random, encryption key.
 * @returns {string}
 */
export function createAutofillKey() {
   
const result = UniFFIScaffolding.callSync(
    15, // uniffi_autofill_fn_func_create_autofill_key
)
return handleRustResult(
    result,
    FfiConverterString.lift.bind(FfiConverterString),
    FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
)
}

/**
 * Decrypt an arbitrary string - `key` must have come from `create_key()`
 * and `ciphertext` must have come from `encrypt_string()`
 * @param {string} key
 * @param {string} ciphertext
 * @returns {string}
 */
export function decryptString(
    key, 
    ciphertext) {
   
FfiConverterString.checkType(key);
FfiConverterString.checkType(ciphertext);
const result = UniFFIScaffolding.callSync(
    16, // uniffi_autofill_fn_func_decrypt_string
    FfiConverterString.lower(key),
    FfiConverterString.lower(ciphertext),
)
return handleRustResult(
    result,
    FfiConverterString.lift.bind(FfiConverterString),
    FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
)
}

/**
 * Encrypt an arbitrary string - `key` must have come from `create_key()`
 * @param {string} key
 * @param {string} cleartext
 * @returns {string}
 */
export function encryptString(
    key, 
    cleartext) {
   
FfiConverterString.checkType(key);
FfiConverterString.checkType(cleartext);
const result = UniFFIScaffolding.callSync(
    17, // uniffi_autofill_fn_func_encrypt_string
    FfiConverterString.lower(key),
    FfiConverterString.lower(cleartext),
)
return handleRustResult(
    result,
    FfiConverterString.lift.bind(FfiConverterString),
    FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
)
}






// Export the FFIConverter object to make external types work.
export class FfiConverterOptionalInt64 extends FfiConverterArrayBuffer {
    static checkType(value) {
        if (value !== undefined && value !== null) {
            FfiConverterInt64.checkType(value)
        }
    }

    static read(dataStream) {
        const code = dataStream.readUint8(0);
        switch (code) {
            case 0:
                return null
            case 1:
                return FfiConverterInt64.read(dataStream)
            default:
                throw new UniFFIError(`Unexpected code: ${code}`);
        }
    }

    static write(dataStream, value) {
        if (value === null || value === undefined) {
            dataStream.writeUint8(0);
            return;
        }
        dataStream.writeUint8(1);
        FfiConverterInt64.write(dataStream, value)
    }

    static computeSize(value) {
        if (value === null || value === undefined) {
            return 1;
        }
        return 1 + FfiConverterInt64.computeSize(value)
    }
}
/**
 * What you get back as an address.
 */
export class Address {
    constructor(
        {
            guid, 
            name, 
            organization, 
            streetAddress, 
            addressLevel3, 
            addressLevel2, 
            addressLevel1, 
            postalCode, 
            country, 
            tel, 
            email, 
            timeCreated, 
            timeLastUsed, 
            timeLastModified, 
            timesUsed
        } = {
            guid: undefined, 
            name: undefined, 
            organization: undefined, 
            streetAddress: undefined, 
            addressLevel3: undefined, 
            addressLevel2: undefined, 
            addressLevel1: undefined, 
            postalCode: undefined, 
            country: undefined, 
            tel: undefined, 
            email: undefined, 
            timeCreated: undefined, 
            timeLastUsed: undefined, 
            timeLastModified: undefined, 
            timesUsed: undefined
        }
    ) {
        try {
            FfiConverterString.checkType(guid)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("guid");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(name)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("name");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(organization)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("organization");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(streetAddress)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("streetAddress");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(addressLevel3)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("addressLevel3");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(addressLevel2)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("addressLevel2");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(addressLevel1)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("addressLevel1");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(postalCode)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("postalCode");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(country)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("country");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(tel)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("tel");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(email)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("email");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(timeCreated)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("timeCreated");
            }
            throw e;
        }
        try {
            FfiConverterOptionalInt64.checkType(timeLastUsed)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("timeLastUsed");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(timeLastModified)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("timeLastModified");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(timesUsed)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("timesUsed");
            }
            throw e;
        }
        /**
         * @type {string}
         */
        this.guid = guid;
        /**
         * @type {string}
         */
        this.name = name;
        /**
         * @type {string}
         */
        this.organization = organization;
        /**
         * @type {string}
         */
        this.streetAddress = streetAddress;
        /**
         * @type {string}
         */
        this.addressLevel3 = addressLevel3;
        /**
         * @type {string}
         */
        this.addressLevel2 = addressLevel2;
        /**
         * @type {string}
         */
        this.addressLevel1 = addressLevel1;
        /**
         * @type {string}
         */
        this.postalCode = postalCode;
        /**
         * @type {string}
         */
        this.country = country;
        /**
         * @type {string}
         */
        this.tel = tel;
        /**
         * @type {string}
         */
        this.email = email;
        /**
         * @type {number}
         */
        this.timeCreated = timeCreated;
        /**
         * @type {?number}
         */
        this.timeLastUsed = timeLastUsed;
        /**
         * @type {number}
         */
        this.timeLastModified = timeLastModified;
        /**
         * @type {number}
         */
        this.timesUsed = timesUsed;
    }

    equals(other) {
        return (
            this.guid == other.guid
            && this.name == other.name
            && this.organization == other.organization
            && this.streetAddress == other.streetAddress
            && this.addressLevel3 == other.addressLevel3
            && this.addressLevel2 == other.addressLevel2
            && this.addressLevel1 == other.addressLevel1
            && this.postalCode == other.postalCode
            && this.country == other.country
            && this.tel == other.tel
            && this.email == other.email
            && this.timeCreated == other.timeCreated
            && this.timeLastUsed == other.timeLastUsed
            && this.timeLastModified == other.timeLastModified
            && this.timesUsed == other.timesUsed
        )
    }
}

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeAddress extends FfiConverterArrayBuffer {
    static read(dataStream) {
        return new Address({
            guid: FfiConverterString.read(dataStream),
            name: FfiConverterString.read(dataStream),
            organization: FfiConverterString.read(dataStream),
            streetAddress: FfiConverterString.read(dataStream),
            addressLevel3: FfiConverterString.read(dataStream),
            addressLevel2: FfiConverterString.read(dataStream),
            addressLevel1: FfiConverterString.read(dataStream),
            postalCode: FfiConverterString.read(dataStream),
            country: FfiConverterString.read(dataStream),
            tel: FfiConverterString.read(dataStream),
            email: FfiConverterString.read(dataStream),
            timeCreated: FfiConverterInt64.read(dataStream),
            timeLastUsed: FfiConverterOptionalInt64.read(dataStream),
            timeLastModified: FfiConverterInt64.read(dataStream),
            timesUsed: FfiConverterInt64.read(dataStream),
        });
    }
    static write(dataStream, value) {
        FfiConverterString.write(dataStream, value.guid);
        FfiConverterString.write(dataStream, value.name);
        FfiConverterString.write(dataStream, value.organization);
        FfiConverterString.write(dataStream, value.streetAddress);
        FfiConverterString.write(dataStream, value.addressLevel3);
        FfiConverterString.write(dataStream, value.addressLevel2);
        FfiConverterString.write(dataStream, value.addressLevel1);
        FfiConverterString.write(dataStream, value.postalCode);
        FfiConverterString.write(dataStream, value.country);
        FfiConverterString.write(dataStream, value.tel);
        FfiConverterString.write(dataStream, value.email);
        FfiConverterInt64.write(dataStream, value.timeCreated);
        FfiConverterOptionalInt64.write(dataStream, value.timeLastUsed);
        FfiConverterInt64.write(dataStream, value.timeLastModified);
        FfiConverterInt64.write(dataStream, value.timesUsed);
    }

    static computeSize(value) {
        let totalSize = 0;
        totalSize += FfiConverterString.computeSize(value.guid);
        totalSize += FfiConverterString.computeSize(value.name);
        totalSize += FfiConverterString.computeSize(value.organization);
        totalSize += FfiConverterString.computeSize(value.streetAddress);
        totalSize += FfiConverterString.computeSize(value.addressLevel3);
        totalSize += FfiConverterString.computeSize(value.addressLevel2);
        totalSize += FfiConverterString.computeSize(value.addressLevel1);
        totalSize += FfiConverterString.computeSize(value.postalCode);
        totalSize += FfiConverterString.computeSize(value.country);
        totalSize += FfiConverterString.computeSize(value.tel);
        totalSize += FfiConverterString.computeSize(value.email);
        totalSize += FfiConverterInt64.computeSize(value.timeCreated);
        totalSize += FfiConverterOptionalInt64.computeSize(value.timeLastUsed);
        totalSize += FfiConverterInt64.computeSize(value.timeLastModified);
        totalSize += FfiConverterInt64.computeSize(value.timesUsed);
        return totalSize
    }

    static checkType(value) {
        super.checkType(value);
        if (!(value instanceof Address)) {
            throw new UniFFITypeError(`Expected 'Address', found '${typeof value}'`);
        }
        try {
            FfiConverterString.checkType(value.guid);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".guid");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.name);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".name");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.organization);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".organization");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.streetAddress);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".streetAddress");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.addressLevel3);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".addressLevel3");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.addressLevel2);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".addressLevel2");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.addressLevel1);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".addressLevel1");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.postalCode);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".postalCode");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.country);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".country");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.tel);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".tel");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.email);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".email");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.timeCreated);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".timeCreated");
            }
            throw e;
        }
        try {
            FfiConverterOptionalInt64.checkType(value.timeLastUsed);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".timeLastUsed");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.timeLastModified);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".timeLastModified");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.timesUsed);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".timesUsed");
            }
            throw e;
        }
    }
}
/**
 * What you get back as a credit-card.
 */
export class CreditCard {
    constructor(
        {
            guid, 
            ccName, 
            ccNumberEnc, 
            ccNumberLast4, 
            ccExpMonth, 
            ccExpYear, 
            ccType, 
            timeCreated, 
            timeLastUsed, 
            timeLastModified, 
            timesUsed
        } = {
            guid: undefined, 
            ccName: undefined, 
            ccNumberEnc: undefined, 
            ccNumberLast4: undefined, 
            ccExpMonth: undefined, 
            ccExpYear: undefined, 
            ccType: undefined, 
            timeCreated: undefined, 
            timeLastUsed: undefined, 
            timeLastModified: undefined, 
            timesUsed: undefined
        }
    ) {
        try {
            FfiConverterString.checkType(guid)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("guid");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(ccName)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("ccName");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(ccNumberEnc)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("ccNumberEnc");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(ccNumberLast4)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("ccNumberLast4");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(ccExpMonth)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("ccExpMonth");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(ccExpYear)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("ccExpYear");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(ccType)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("ccType");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(timeCreated)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("timeCreated");
            }
            throw e;
        }
        try {
            FfiConverterOptionalInt64.checkType(timeLastUsed)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("timeLastUsed");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(timeLastModified)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("timeLastModified");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(timesUsed)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("timesUsed");
            }
            throw e;
        }
        /**
         * @type {string}
         */
        this.guid = guid;
        /**
         * @type {string}
         */
        this.ccName = ccName;
        /**
         * @type {string}
         */
        this.ccNumberEnc = ccNumberEnc;
        /**
         * @type {string}
         */
        this.ccNumberLast4 = ccNumberLast4;
        /**
         * @type {number}
         */
        this.ccExpMonth = ccExpMonth;
        /**
         * @type {number}
         */
        this.ccExpYear = ccExpYear;
        /**
         * @type {string}
         */
        this.ccType = ccType;
        /**
         * @type {number}
         */
        this.timeCreated = timeCreated;
        /**
         * @type {?number}
         */
        this.timeLastUsed = timeLastUsed;
        /**
         * @type {number}
         */
        this.timeLastModified = timeLastModified;
        /**
         * @type {number}
         */
        this.timesUsed = timesUsed;
    }

    equals(other) {
        return (
            this.guid == other.guid
            && this.ccName == other.ccName
            && this.ccNumberEnc == other.ccNumberEnc
            && this.ccNumberLast4 == other.ccNumberLast4
            && this.ccExpMonth == other.ccExpMonth
            && this.ccExpYear == other.ccExpYear
            && this.ccType == other.ccType
            && this.timeCreated == other.timeCreated
            && this.timeLastUsed == other.timeLastUsed
            && this.timeLastModified == other.timeLastModified
            && this.timesUsed == other.timesUsed
        )
    }
}

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeCreditCard extends FfiConverterArrayBuffer {
    static read(dataStream) {
        return new CreditCard({
            guid: FfiConverterString.read(dataStream),
            ccName: FfiConverterString.read(dataStream),
            ccNumberEnc: FfiConverterString.read(dataStream),
            ccNumberLast4: FfiConverterString.read(dataStream),
            ccExpMonth: FfiConverterInt64.read(dataStream),
            ccExpYear: FfiConverterInt64.read(dataStream),
            ccType: FfiConverterString.read(dataStream),
            timeCreated: FfiConverterInt64.read(dataStream),
            timeLastUsed: FfiConverterOptionalInt64.read(dataStream),
            timeLastModified: FfiConverterInt64.read(dataStream),
            timesUsed: FfiConverterInt64.read(dataStream),
        });
    }
    static write(dataStream, value) {
        FfiConverterString.write(dataStream, value.guid);
        FfiConverterString.write(dataStream, value.ccName);
        FfiConverterString.write(dataStream, value.ccNumberEnc);
        FfiConverterString.write(dataStream, value.ccNumberLast4);
        FfiConverterInt64.write(dataStream, value.ccExpMonth);
        FfiConverterInt64.write(dataStream, value.ccExpYear);
        FfiConverterString.write(dataStream, value.ccType);
        FfiConverterInt64.write(dataStream, value.timeCreated);
        FfiConverterOptionalInt64.write(dataStream, value.timeLastUsed);
        FfiConverterInt64.write(dataStream, value.timeLastModified);
        FfiConverterInt64.write(dataStream, value.timesUsed);
    }

    static computeSize(value) {
        let totalSize = 0;
        totalSize += FfiConverterString.computeSize(value.guid);
        totalSize += FfiConverterString.computeSize(value.ccName);
        totalSize += FfiConverterString.computeSize(value.ccNumberEnc);
        totalSize += FfiConverterString.computeSize(value.ccNumberLast4);
        totalSize += FfiConverterInt64.computeSize(value.ccExpMonth);
        totalSize += FfiConverterInt64.computeSize(value.ccExpYear);
        totalSize += FfiConverterString.computeSize(value.ccType);
        totalSize += FfiConverterInt64.computeSize(value.timeCreated);
        totalSize += FfiConverterOptionalInt64.computeSize(value.timeLastUsed);
        totalSize += FfiConverterInt64.computeSize(value.timeLastModified);
        totalSize += FfiConverterInt64.computeSize(value.timesUsed);
        return totalSize
    }

    static checkType(value) {
        super.checkType(value);
        if (!(value instanceof CreditCard)) {
            throw new UniFFITypeError(`Expected 'CreditCard', found '${typeof value}'`);
        }
        try {
            FfiConverterString.checkType(value.guid);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".guid");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.ccName);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".ccName");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.ccNumberEnc);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".ccNumberEnc");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.ccNumberLast4);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".ccNumberLast4");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.ccExpMonth);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".ccExpMonth");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.ccExpYear);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".ccExpYear");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.ccType);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".ccType");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.timeCreated);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".timeCreated");
            }
            throw e;
        }
        try {
            FfiConverterOptionalInt64.checkType(value.timeLastUsed);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".timeLastUsed");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.timeLastModified);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".timeLastModified");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.timesUsed);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".timesUsed");
            }
            throw e;
        }
    }
}


/**
 * Metrics tracking scrubbing of credit cards that cannot be decrypted, see
 */
export class CreditCardsDeletionMetrics {
    constructor(
        {
            totalScrubbedRecords
        } = {
            totalScrubbedRecords: undefined
        }
    ) {
        try {
            FfiConverterUInt64.checkType(totalScrubbedRecords)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("totalScrubbedRecords");
            }
            throw e;
        }
        /**
         * @type {number}
         */
        this.totalScrubbedRecords = totalScrubbedRecords;
    }

    equals(other) {
        return (
            this.totalScrubbedRecords == other.totalScrubbedRecords
        )
    }
}

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeCreditCardsDeletionMetrics extends FfiConverterArrayBuffer {
    static read(dataStream) {
        return new CreditCardsDeletionMetrics({
            totalScrubbedRecords: FfiConverterUInt64.read(dataStream),
        });
    }
    static write(dataStream, value) {
        FfiConverterUInt64.write(dataStream, value.totalScrubbedRecords);
    }

    static computeSize(value) {
        let totalSize = 0;
        totalSize += FfiConverterUInt64.computeSize(value.totalScrubbedRecords);
        return totalSize
    }

    static checkType(value) {
        super.checkType(value);
        if (!(value instanceof CreditCardsDeletionMetrics)) {
            throw new UniFFITypeError(`Expected 'CreditCardsDeletionMetrics', found '${typeof value}'`);
        }
        try {
            FfiConverterUInt64.checkType(value.totalScrubbedRecords);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".totalScrubbedRecords");
            }
            throw e;
        }
    }
}
/**
 * What you get back as a passport.
 */
export class Passport {
    constructor(
        {
            guid, 
            name, 
            country, 
            passportNumber, 
            issueDateMonth, 
            issueDateDay, 
            issueDateYear, 
            expiryDateMonth, 
            expiryDateDay, 
            expiryDateYear, 
            timeCreated, 
            timeLastUsed, 
            timeLastModified, 
            timesUsed
        } = {
            guid: undefined, 
            name: undefined, 
            country: undefined, 
            passportNumber: undefined, 
            issueDateMonth: undefined, 
            issueDateDay: undefined, 
            issueDateYear: undefined, 
            expiryDateMonth: undefined, 
            expiryDateDay: undefined, 
            expiryDateYear: undefined, 
            timeCreated: undefined, 
            timeLastUsed: undefined, 
            timeLastModified: undefined, 
            timesUsed: undefined
        }
    ) {
        try {
            FfiConverterString.checkType(guid)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("guid");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(name)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("name");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(country)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("country");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(passportNumber)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("passportNumber");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(issueDateMonth)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("issueDateMonth");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(issueDateDay)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("issueDateDay");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(issueDateYear)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("issueDateYear");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(expiryDateMonth)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("expiryDateMonth");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(expiryDateDay)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("expiryDateDay");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(expiryDateYear)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("expiryDateYear");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(timeCreated)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("timeCreated");
            }
            throw e;
        }
        try {
            FfiConverterOptionalInt64.checkType(timeLastUsed)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("timeLastUsed");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(timeLastModified)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("timeLastModified");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(timesUsed)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("timesUsed");
            }
            throw e;
        }
        /**
         * @type {string}
         */
        this.guid = guid;
        /**
         * @type {string}
         */
        this.name = name;
        /**
         * @type {string}
         */
        this.country = country;
        /**
         * @type {string}
         */
        this.passportNumber = passportNumber;
        /**
         * @type {number}
         */
        this.issueDateMonth = issueDateMonth;
        /**
         * @type {number}
         */
        this.issueDateDay = issueDateDay;
        /**
         * @type {number}
         */
        this.issueDateYear = issueDateYear;
        /**
         * @type {number}
         */
        this.expiryDateMonth = expiryDateMonth;
        /**
         * @type {number}
         */
        this.expiryDateDay = expiryDateDay;
        /**
         * @type {number}
         */
        this.expiryDateYear = expiryDateYear;
        /**
         * @type {number}
         */
        this.timeCreated = timeCreated;
        /**
         * @type {?number}
         */
        this.timeLastUsed = timeLastUsed;
        /**
         * @type {number}
         */
        this.timeLastModified = timeLastModified;
        /**
         * @type {number}
         */
        this.timesUsed = timesUsed;
    }

    equals(other) {
        return (
            this.guid == other.guid
            && this.name == other.name
            && this.country == other.country
            && this.passportNumber == other.passportNumber
            && this.issueDateMonth == other.issueDateMonth
            && this.issueDateDay == other.issueDateDay
            && this.issueDateYear == other.issueDateYear
            && this.expiryDateMonth == other.expiryDateMonth
            && this.expiryDateDay == other.expiryDateDay
            && this.expiryDateYear == other.expiryDateYear
            && this.timeCreated == other.timeCreated
            && this.timeLastUsed == other.timeLastUsed
            && this.timeLastModified == other.timeLastModified
            && this.timesUsed == other.timesUsed
        )
    }
}

// Export the FFIConverter object to make external types work.
export class FfiConverterTypePassport extends FfiConverterArrayBuffer {
    static read(dataStream) {
        return new Passport({
            guid: FfiConverterString.read(dataStream),
            name: FfiConverterString.read(dataStream),
            country: FfiConverterString.read(dataStream),
            passportNumber: FfiConverterString.read(dataStream),
            issueDateMonth: FfiConverterInt64.read(dataStream),
            issueDateDay: FfiConverterInt64.read(dataStream),
            issueDateYear: FfiConverterInt64.read(dataStream),
            expiryDateMonth: FfiConverterInt64.read(dataStream),
            expiryDateDay: FfiConverterInt64.read(dataStream),
            expiryDateYear: FfiConverterInt64.read(dataStream),
            timeCreated: FfiConverterInt64.read(dataStream),
            timeLastUsed: FfiConverterOptionalInt64.read(dataStream),
            timeLastModified: FfiConverterInt64.read(dataStream),
            timesUsed: FfiConverterInt64.read(dataStream),
        });
    }
    static write(dataStream, value) {
        FfiConverterString.write(dataStream, value.guid);
        FfiConverterString.write(dataStream, value.name);
        FfiConverterString.write(dataStream, value.country);
        FfiConverterString.write(dataStream, value.passportNumber);
        FfiConverterInt64.write(dataStream, value.issueDateMonth);
        FfiConverterInt64.write(dataStream, value.issueDateDay);
        FfiConverterInt64.write(dataStream, value.issueDateYear);
        FfiConverterInt64.write(dataStream, value.expiryDateMonth);
        FfiConverterInt64.write(dataStream, value.expiryDateDay);
        FfiConverterInt64.write(dataStream, value.expiryDateYear);
        FfiConverterInt64.write(dataStream, value.timeCreated);
        FfiConverterOptionalInt64.write(dataStream, value.timeLastUsed);
        FfiConverterInt64.write(dataStream, value.timeLastModified);
        FfiConverterInt64.write(dataStream, value.timesUsed);
    }

    static computeSize(value) {
        let totalSize = 0;
        totalSize += FfiConverterString.computeSize(value.guid);
        totalSize += FfiConverterString.computeSize(value.name);
        totalSize += FfiConverterString.computeSize(value.country);
        totalSize += FfiConverterString.computeSize(value.passportNumber);
        totalSize += FfiConverterInt64.computeSize(value.issueDateMonth);
        totalSize += FfiConverterInt64.computeSize(value.issueDateDay);
        totalSize += FfiConverterInt64.computeSize(value.issueDateYear);
        totalSize += FfiConverterInt64.computeSize(value.expiryDateMonth);
        totalSize += FfiConverterInt64.computeSize(value.expiryDateDay);
        totalSize += FfiConverterInt64.computeSize(value.expiryDateYear);
        totalSize += FfiConverterInt64.computeSize(value.timeCreated);
        totalSize += FfiConverterOptionalInt64.computeSize(value.timeLastUsed);
        totalSize += FfiConverterInt64.computeSize(value.timeLastModified);
        totalSize += FfiConverterInt64.computeSize(value.timesUsed);
        return totalSize
    }

    static checkType(value) {
        super.checkType(value);
        if (!(value instanceof Passport)) {
            throw new UniFFITypeError(`Expected 'Passport', found '${typeof value}'`);
        }
        try {
            FfiConverterString.checkType(value.guid);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".guid");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.name);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".name");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.country);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".country");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.passportNumber);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".passportNumber");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.issueDateMonth);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".issueDateMonth");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.issueDateDay);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".issueDateDay");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.issueDateYear);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".issueDateYear");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.expiryDateMonth);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".expiryDateMonth");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.expiryDateDay);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".expiryDateDay");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.expiryDateYear);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".expiryDateYear");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.timeCreated);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".timeCreated");
            }
            throw e;
        }
        try {
            FfiConverterOptionalInt64.checkType(value.timeLastUsed);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".timeLastUsed");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.timeLastModified);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".timeLastModified");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.timesUsed);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".timesUsed");
            }
            throw e;
        }
    }
}
/**
 * What you pass to create or update an address.
 */
export class UpdatableAddressFields {
    constructor(
        {
            name, 
            organization, 
            streetAddress, 
            addressLevel3, 
            addressLevel2, 
            addressLevel1, 
            postalCode, 
            country, 
            tel, 
            email
        } = {
            name: undefined, 
            organization: undefined, 
            streetAddress: undefined, 
            addressLevel3: undefined, 
            addressLevel2: undefined, 
            addressLevel1: undefined, 
            postalCode: undefined, 
            country: undefined, 
            tel: undefined, 
            email: undefined
        }
    ) {
        try {
            FfiConverterString.checkType(name)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("name");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(organization)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("organization");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(streetAddress)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("streetAddress");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(addressLevel3)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("addressLevel3");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(addressLevel2)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("addressLevel2");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(addressLevel1)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("addressLevel1");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(postalCode)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("postalCode");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(country)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("country");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(tel)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("tel");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(email)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("email");
            }
            throw e;
        }
        /**
         * @type {string}
         */
        this.name = name;
        /**
         * @type {string}
         */
        this.organization = organization;
        /**
         * @type {string}
         */
        this.streetAddress = streetAddress;
        /**
         * @type {string}
         */
        this.addressLevel3 = addressLevel3;
        /**
         * @type {string}
         */
        this.addressLevel2 = addressLevel2;
        /**
         * @type {string}
         */
        this.addressLevel1 = addressLevel1;
        /**
         * @type {string}
         */
        this.postalCode = postalCode;
        /**
         * @type {string}
         */
        this.country = country;
        /**
         * @type {string}
         */
        this.tel = tel;
        /**
         * @type {string}
         */
        this.email = email;
    }

    equals(other) {
        return (
            this.name == other.name
            && this.organization == other.organization
            && this.streetAddress == other.streetAddress
            && this.addressLevel3 == other.addressLevel3
            && this.addressLevel2 == other.addressLevel2
            && this.addressLevel1 == other.addressLevel1
            && this.postalCode == other.postalCode
            && this.country == other.country
            && this.tel == other.tel
            && this.email == other.email
        )
    }
}

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeUpdatableAddressFields extends FfiConverterArrayBuffer {
    static read(dataStream) {
        return new UpdatableAddressFields({
            name: FfiConverterString.read(dataStream),
            organization: FfiConverterString.read(dataStream),
            streetAddress: FfiConverterString.read(dataStream),
            addressLevel3: FfiConverterString.read(dataStream),
            addressLevel2: FfiConverterString.read(dataStream),
            addressLevel1: FfiConverterString.read(dataStream),
            postalCode: FfiConverterString.read(dataStream),
            country: FfiConverterString.read(dataStream),
            tel: FfiConverterString.read(dataStream),
            email: FfiConverterString.read(dataStream),
        });
    }
    static write(dataStream, value) {
        FfiConverterString.write(dataStream, value.name);
        FfiConverterString.write(dataStream, value.organization);
        FfiConverterString.write(dataStream, value.streetAddress);
        FfiConverterString.write(dataStream, value.addressLevel3);
        FfiConverterString.write(dataStream, value.addressLevel2);
        FfiConverterString.write(dataStream, value.addressLevel1);
        FfiConverterString.write(dataStream, value.postalCode);
        FfiConverterString.write(dataStream, value.country);
        FfiConverterString.write(dataStream, value.tel);
        FfiConverterString.write(dataStream, value.email);
    }

    static computeSize(value) {
        let totalSize = 0;
        totalSize += FfiConverterString.computeSize(value.name);
        totalSize += FfiConverterString.computeSize(value.organization);
        totalSize += FfiConverterString.computeSize(value.streetAddress);
        totalSize += FfiConverterString.computeSize(value.addressLevel3);
        totalSize += FfiConverterString.computeSize(value.addressLevel2);
        totalSize += FfiConverterString.computeSize(value.addressLevel1);
        totalSize += FfiConverterString.computeSize(value.postalCode);
        totalSize += FfiConverterString.computeSize(value.country);
        totalSize += FfiConverterString.computeSize(value.tel);
        totalSize += FfiConverterString.computeSize(value.email);
        return totalSize
    }

    static checkType(value) {
        super.checkType(value);
        if (!(value instanceof UpdatableAddressFields)) {
            throw new UniFFITypeError(`Expected 'UpdatableAddressFields', found '${typeof value}'`);
        }
        try {
            FfiConverterString.checkType(value.name);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".name");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.organization);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".organization");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.streetAddress);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".streetAddress");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.addressLevel3);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".addressLevel3");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.addressLevel2);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".addressLevel2");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.addressLevel1);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".addressLevel1");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.postalCode);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".postalCode");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.country);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".country");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.tel);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".tel");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.email);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".email");
            }
            throw e;
        }
    }
}
/**
 * What you pass to create or update a credit-card.
 */
export class UpdatableCreditCardFields {
    constructor(
        {
            ccName, 
            ccNumberEnc, 
            ccNumberLast4, 
            ccExpMonth, 
            ccExpYear, 
            ccType
        } = {
            ccName: undefined, 
            ccNumberEnc: undefined, 
            ccNumberLast4: undefined, 
            ccExpMonth: undefined, 
            ccExpYear: undefined, 
            ccType: undefined
        }
    ) {
        try {
            FfiConverterString.checkType(ccName)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("ccName");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(ccNumberEnc)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("ccNumberEnc");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(ccNumberLast4)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("ccNumberLast4");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(ccExpMonth)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("ccExpMonth");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(ccExpYear)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("ccExpYear");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(ccType)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("ccType");
            }
            throw e;
        }
        /**
         * @type {string}
         */
        this.ccName = ccName;
        /**
         * @type {string}
         */
        this.ccNumberEnc = ccNumberEnc;
        /**
         * @type {string}
         */
        this.ccNumberLast4 = ccNumberLast4;
        /**
         * @type {number}
         */
        this.ccExpMonth = ccExpMonth;
        /**
         * @type {number}
         */
        this.ccExpYear = ccExpYear;
        /**
         * @type {string}
         */
        this.ccType = ccType;
    }

    equals(other) {
        return (
            this.ccName == other.ccName
            && this.ccNumberEnc == other.ccNumberEnc
            && this.ccNumberLast4 == other.ccNumberLast4
            && this.ccExpMonth == other.ccExpMonth
            && this.ccExpYear == other.ccExpYear
            && this.ccType == other.ccType
        )
    }
}

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeUpdatableCreditCardFields extends FfiConverterArrayBuffer {
    static read(dataStream) {
        return new UpdatableCreditCardFields({
            ccName: FfiConverterString.read(dataStream),
            ccNumberEnc: FfiConverterString.read(dataStream),
            ccNumberLast4: FfiConverterString.read(dataStream),
            ccExpMonth: FfiConverterInt64.read(dataStream),
            ccExpYear: FfiConverterInt64.read(dataStream),
            ccType: FfiConverterString.read(dataStream),
        });
    }
    static write(dataStream, value) {
        FfiConverterString.write(dataStream, value.ccName);
        FfiConverterString.write(dataStream, value.ccNumberEnc);
        FfiConverterString.write(dataStream, value.ccNumberLast4);
        FfiConverterInt64.write(dataStream, value.ccExpMonth);
        FfiConverterInt64.write(dataStream, value.ccExpYear);
        FfiConverterString.write(dataStream, value.ccType);
    }

    static computeSize(value) {
        let totalSize = 0;
        totalSize += FfiConverterString.computeSize(value.ccName);
        totalSize += FfiConverterString.computeSize(value.ccNumberEnc);
        totalSize += FfiConverterString.computeSize(value.ccNumberLast4);
        totalSize += FfiConverterInt64.computeSize(value.ccExpMonth);
        totalSize += FfiConverterInt64.computeSize(value.ccExpYear);
        totalSize += FfiConverterString.computeSize(value.ccType);
        return totalSize
    }

    static checkType(value) {
        super.checkType(value);
        if (!(value instanceof UpdatableCreditCardFields)) {
            throw new UniFFITypeError(`Expected 'UpdatableCreditCardFields', found '${typeof value}'`);
        }
        try {
            FfiConverterString.checkType(value.ccName);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".ccName");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.ccNumberEnc);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".ccNumberEnc");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.ccNumberLast4);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".ccNumberLast4");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.ccExpMonth);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".ccExpMonth");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.ccExpYear);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".ccExpYear");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.ccType);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".ccType");
            }
            throw e;
        }
    }
}
/**
 * What you pass to create or update a passport.
 */
export class UpdatablePassportFields {
    constructor(
        {
            name, 
            country, 
            passportNumber, 
            issueDateMonth, 
            issueDateDay, 
            issueDateYear, 
            expiryDateMonth, 
            expiryDateDay, 
            expiryDateYear
        } = {
            name: undefined, 
            country: undefined, 
            passportNumber: undefined, 
            issueDateMonth: undefined, 
            issueDateDay: undefined, 
            issueDateYear: undefined, 
            expiryDateMonth: undefined, 
            expiryDateDay: undefined, 
            expiryDateYear: undefined
        }
    ) {
        try {
            FfiConverterString.checkType(name)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("name");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(country)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("country");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(passportNumber)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("passportNumber");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(issueDateMonth)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("issueDateMonth");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(issueDateDay)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("issueDateDay");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(issueDateYear)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("issueDateYear");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(expiryDateMonth)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("expiryDateMonth");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(expiryDateDay)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("expiryDateDay");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(expiryDateYear)
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart("expiryDateYear");
            }
            throw e;
        }
        /**
         * @type {string}
         */
        this.name = name;
        /**
         * @type {string}
         */
        this.country = country;
        /**
         * @type {string}
         */
        this.passportNumber = passportNumber;
        /**
         * @type {number}
         */
        this.issueDateMonth = issueDateMonth;
        /**
         * @type {number}
         */
        this.issueDateDay = issueDateDay;
        /**
         * @type {number}
         */
        this.issueDateYear = issueDateYear;
        /**
         * @type {number}
         */
        this.expiryDateMonth = expiryDateMonth;
        /**
         * @type {number}
         */
        this.expiryDateDay = expiryDateDay;
        /**
         * @type {number}
         */
        this.expiryDateYear = expiryDateYear;
    }

    equals(other) {
        return (
            this.name == other.name
            && this.country == other.country
            && this.passportNumber == other.passportNumber
            && this.issueDateMonth == other.issueDateMonth
            && this.issueDateDay == other.issueDateDay
            && this.issueDateYear == other.issueDateYear
            && this.expiryDateMonth == other.expiryDateMonth
            && this.expiryDateDay == other.expiryDateDay
            && this.expiryDateYear == other.expiryDateYear
        )
    }
}

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeUpdatablePassportFields extends FfiConverterArrayBuffer {
    static read(dataStream) {
        return new UpdatablePassportFields({
            name: FfiConverterString.read(dataStream),
            country: FfiConverterString.read(dataStream),
            passportNumber: FfiConverterString.read(dataStream),
            issueDateMonth: FfiConverterInt64.read(dataStream),
            issueDateDay: FfiConverterInt64.read(dataStream),
            issueDateYear: FfiConverterInt64.read(dataStream),
            expiryDateMonth: FfiConverterInt64.read(dataStream),
            expiryDateDay: FfiConverterInt64.read(dataStream),
            expiryDateYear: FfiConverterInt64.read(dataStream),
        });
    }
    static write(dataStream, value) {
        FfiConverterString.write(dataStream, value.name);
        FfiConverterString.write(dataStream, value.country);
        FfiConverterString.write(dataStream, value.passportNumber);
        FfiConverterInt64.write(dataStream, value.issueDateMonth);
        FfiConverterInt64.write(dataStream, value.issueDateDay);
        FfiConverterInt64.write(dataStream, value.issueDateYear);
        FfiConverterInt64.write(dataStream, value.expiryDateMonth);
        FfiConverterInt64.write(dataStream, value.expiryDateDay);
        FfiConverterInt64.write(dataStream, value.expiryDateYear);
    }

    static computeSize(value) {
        let totalSize = 0;
        totalSize += FfiConverterString.computeSize(value.name);
        totalSize += FfiConverterString.computeSize(value.country);
        totalSize += FfiConverterString.computeSize(value.passportNumber);
        totalSize += FfiConverterInt64.computeSize(value.issueDateMonth);
        totalSize += FfiConverterInt64.computeSize(value.issueDateDay);
        totalSize += FfiConverterInt64.computeSize(value.issueDateYear);
        totalSize += FfiConverterInt64.computeSize(value.expiryDateMonth);
        totalSize += FfiConverterInt64.computeSize(value.expiryDateDay);
        totalSize += FfiConverterInt64.computeSize(value.expiryDateYear);
        return totalSize
    }

    static checkType(value) {
        super.checkType(value);
        if (!(value instanceof UpdatablePassportFields)) {
            throw new UniFFITypeError(`Expected 'UpdatablePassportFields', found '${typeof value}'`);
        }
        try {
            FfiConverterString.checkType(value.name);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".name");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.country);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".country");
            }
            throw e;
        }
        try {
            FfiConverterString.checkType(value.passportNumber);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".passportNumber");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.issueDateMonth);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".issueDateMonth");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.issueDateDay);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".issueDateDay");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.issueDateYear);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".issueDateYear");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.expiryDateMonth);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".expiryDateMonth");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.expiryDateDay);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".expiryDateDay");
            }
            throw e;
        }
        try {
            FfiConverterInt64.checkType(value.expiryDateYear);
        } catch (e) {
            if (e instanceof UniFFITypeError) {
                e.addItemDescriptionPart(".expiryDateYear");
            }
            throw e;
        }
    }
}

/**
 * AutofillApiError
 */
export class AutofillApiError extends Error {}


/**
 * SqlError
 */
export class SqlError extends AutofillApiError {

    constructor(
        reason,
        ...params
    ) {
        const message = `reason: ${ reason }`;
        super(message, ...params);
        this.reason = reason;
    }
    toString() {
        return `SqlError: ${super.toString()}`
    }
}

/**
 * InterruptedError
 */
export class InterruptedError extends AutofillApiError {

    constructor(
        ...params
    ) {
        super(...params);
    }
    toString() {
        return `InterruptedError: ${super.toString()}`
    }
}

/**
 * CryptoError
 */
export class CryptoError extends AutofillApiError {

    constructor(
        reason,
        ...params
    ) {
        const message = `reason: ${ reason }`;
        super(message, ...params);
        this.reason = reason;
    }
    toString() {
        return `CryptoError: ${super.toString()}`
    }
}

/**
 * NoSuchRecord
 */
export class NoSuchRecord extends AutofillApiError {

    constructor(
        guid,
        ...params
    ) {
        const message = `guid: ${ guid }`;
        super(message, ...params);
        this.guid = guid;
    }
    toString() {
        return `NoSuchRecord: ${super.toString()}`
    }
}

/**
 * UnexpectedAutofillApiError
 */
export class UnexpectedAutofillApiError extends AutofillApiError {

    constructor(
        reason,
        ...params
    ) {
        const message = `reason: ${ reason }`;
        super(message, ...params);
        this.reason = reason;
    }
    toString() {
        return `UnexpectedAutofillApiError: ${super.toString()}`
    }
}

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeAutofillApiError extends FfiConverterArrayBuffer {
    static read(dataStream) {
        switch (dataStream.readInt32()) {
            case 1:
                return new SqlError(
                    FfiConverterString.read(dataStream)
                    );
            case 2:
                return new InterruptedError(
                    );
            case 3:
                return new CryptoError(
                    FfiConverterString.read(dataStream)
                    );
            case 4:
                return new NoSuchRecord(
                    FfiConverterString.read(dataStream)
                    );
            case 5:
                return new UnexpectedAutofillApiError(
                    FfiConverterString.read(dataStream)
                    );
            default:
                throw new UniFFITypeError("Unknown AutofillApiError variant");
        }
    }
    static computeSize(value) {
        // Size of the Int indicating the variant
        let totalSize = 4;
        if (value instanceof SqlError) {
            totalSize += FfiConverterString.computeSize(value.reason);
            return totalSize;
        }
        if (value instanceof InterruptedError) {
            return totalSize;
        }
        if (value instanceof CryptoError) {
            totalSize += FfiConverterString.computeSize(value.reason);
            return totalSize;
        }
        if (value instanceof NoSuchRecord) {
            totalSize += FfiConverterString.computeSize(value.guid);
            return totalSize;
        }
        if (value instanceof UnexpectedAutofillApiError) {
            totalSize += FfiConverterString.computeSize(value.reason);
            return totalSize;
        }
        throw new UniFFITypeError("Unknown AutofillApiError variant");
    }
    static write(dataStream, value) {
        if (value instanceof SqlError) {
            dataStream.writeInt32(1);
            FfiConverterString.write(dataStream, value.reason);
            return;
        }
        if (value instanceof InterruptedError) {
            dataStream.writeInt32(2);
            return;
        }
        if (value instanceof CryptoError) {
            dataStream.writeInt32(3);
            FfiConverterString.write(dataStream, value.reason);
            return;
        }
        if (value instanceof NoSuchRecord) {
            dataStream.writeInt32(4);
            FfiConverterString.write(dataStream, value.guid);
            return;
        }
        if (value instanceof UnexpectedAutofillApiError) {
            dataStream.writeInt32(5);
            FfiConverterString.write(dataStream, value.reason);
            return;
        }
        throw new UniFFITypeError("Unknown AutofillApiError variant");
    }

    static errorClass = AutofillApiError;
}


// Export the FFIConverter object to make external types work.
export class FfiConverterSequenceTypeAddress extends FfiConverterArrayBuffer {
    static read(dataStream) {
        const len = dataStream.readInt32();
        const arr = [];
        for (let i = 0; i < len; i++) {
            arr.push(FfiConverterTypeAddress.read(dataStream));
        }
        return arr;
    }

    static write(dataStream, value) {
        dataStream.writeInt32(value.length);
        value.forEach((innerValue) => {
            FfiConverterTypeAddress.write(dataStream, innerValue);
        })
    }

    static computeSize(value) {
        // The size of the length
        let size = 4;
        for (const innerValue of value) {
            size += FfiConverterTypeAddress.computeSize(innerValue);
        }
        return size;
    }

    static checkType(value) {
        if (!Array.isArray(value)) {
            throw new UniFFITypeError(`${value} is not an array`);
        }
        value.forEach((innerValue, idx) => {
            try {
                FfiConverterTypeAddress.checkType(innerValue);
            } catch (e) {
                if (e instanceof UniFFITypeError) {
                    e.addItemDescriptionPart(`[${idx}]`);
                }
                throw e;
            }
        })
    }
}
// Export the FFIConverter object to make external types work.
export class FfiConverterSequenceTypeCreditCard extends FfiConverterArrayBuffer {
    static read(dataStream) {
        const len = dataStream.readInt32();
        const arr = [];
        for (let i = 0; i < len; i++) {
            arr.push(FfiConverterTypeCreditCard.read(dataStream));
        }
        return arr;
    }

    static write(dataStream, value) {
        dataStream.writeInt32(value.length);
        value.forEach((innerValue) => {
            FfiConverterTypeCreditCard.write(dataStream, innerValue);
        })
    }

    static computeSize(value) {
        // The size of the length
        let size = 4;
        for (const innerValue of value) {
            size += FfiConverterTypeCreditCard.computeSize(innerValue);
        }
        return size;
    }

    static checkType(value) {
        if (!Array.isArray(value)) {
            throw new UniFFITypeError(`${value} is not an array`);
        }
        value.forEach((innerValue, idx) => {
            try {
                FfiConverterTypeCreditCard.checkType(innerValue);
            } catch (e) {
                if (e instanceof UniFFITypeError) {
                    e.addItemDescriptionPart(`[${idx}]`);
                }
                throw e;
            }
        })
    }
}
// Export the FFIConverter object to make external types work.
export class FfiConverterSequenceTypePassport extends FfiConverterArrayBuffer {
    static read(dataStream) {
        const len = dataStream.readInt32();
        const arr = [];
        for (let i = 0; i < len; i++) {
            arr.push(FfiConverterTypePassport.read(dataStream));
        }
        return arr;
    }

    static write(dataStream, value) {
        dataStream.writeInt32(value.length);
        value.forEach((innerValue) => {
            FfiConverterTypePassport.write(dataStream, innerValue);
        })
    }

    static computeSize(value) {
        // The size of the length
        let size = 4;
        for (const innerValue of value) {
            size += FfiConverterTypePassport.computeSize(innerValue);
        }
        return size;
    }

    static checkType(value) {
        if (!Array.isArray(value)) {
            throw new UniFFITypeError(`${value} is not an array`);
        }
        value.forEach((innerValue, idx) => {
            try {
                FfiConverterTypePassport.checkType(innerValue);
            } catch (e) {
                if (e instanceof UniFFITypeError) {
                    e.addItemDescriptionPart(`[${idx}]`);
                }
                throw e;
            }
        })
    }
}

/**
 * StoreInterface
 */
export class StoreInterface {
    /**
     * addAddress
     * @param {UpdatableAddressFields} a
     * @returns {Promise<Address>}}
     */
    async addAddress(
        a) {
      throw Error("addAddress not implemented");
    }
    /**
     * addCreditCard
     * @param {UpdatableCreditCardFields} cc
     * @returns {Promise<CreditCard>}}
     */
    async addCreditCard(
        cc) {
      throw Error("addCreditCard not implemented");
    }
    /**
     * addPassport
     * @param {UpdatablePassportFields} p
     * @returns {Promise<Passport>}}
     */
    async addPassport(
        p) {
      throw Error("addPassport not implemented");
    }
    /**
     * countAllAddresses
     * @returns {Promise<number>}}
     */
    async countAllAddresses() {
      throw Error("countAllAddresses not implemented");
    }
    /**
     * countAllCreditCards
     * @returns {Promise<number>}}
     */
    async countAllCreditCards() {
      throw Error("countAllCreditCards not implemented");
    }
    /**
     * countAllPassports
     * @returns {Promise<number>}}
     */
    async countAllPassports() {
      throw Error("countAllPassports not implemented");
    }
    /**
     * deleteAddress
     * @param {string} guid
     * @returns {Promise<boolean>}}
     */
    async deleteAddress(
        guid) {
      throw Error("deleteAddress not implemented");
    }
    /**
     * deleteCreditCard
     * @param {string} guid
     * @returns {Promise<boolean>}}
     */
    async deleteCreditCard(
        guid) {
      throw Error("deleteCreditCard not implemented");
    }
    /**
     * deletePassport
     * @param {string} guid
     * @returns {Promise<boolean>}}
     */
    async deletePassport(
        guid) {
      throw Error("deletePassport not implemented");
    }
    /**
     * getAddress
     * @param {string} guid
     * @returns {Promise<Address>}}
     */
    async getAddress(
        guid) {
      throw Error("getAddress not implemented");
    }
    /**
     * getAllAddresses
     * @returns {Promise<Array.<Address>>}}
     */
    async getAllAddresses() {
      throw Error("getAllAddresses not implemented");
    }
    /**
     * getAllCreditCards
     * @returns {Promise<Array.<CreditCard>>}}
     */
    async getAllCreditCards() {
      throw Error("getAllCreditCards not implemented");
    }
    /**
     * getAllPassports
     * @returns {Promise<Array.<Passport>>}}
     */
    async getAllPassports() {
      throw Error("getAllPassports not implemented");
    }
    /**
     * getCreditCard
     * @param {string} guid
     * @returns {Promise<CreditCard>}}
     */
    async getCreditCard(
        guid) {
      throw Error("getCreditCard not implemented");
    }
    /**
     * getPassport
     * @param {string} guid
     * @returns {Promise<Passport>}}
     */
    async getPassport(
        guid) {
      throw Error("getPassport not implemented");
    }
    /**
     * registerWithSyncManager
     */
    async registerWithSyncManager() {
      throw Error("registerWithSyncManager not implemented");
    }
    /**
     * Run maintenance on the DB
     * 
     * This is intended to be run during idle time and will take steps / to clean up / shrink the
     * database.
     */
    async runMaintenance() {
      throw Error("runMaintenance not implemented");
    }
    /**
     * scrubEncryptedData
     */
    async scrubEncryptedData() {
      throw Error("scrubEncryptedData not implemented");
    }
    /**
     * The `scrub_undecryptable_credit_card_data_for_remote_replacement` function locally scrubs credit cards
     * that cannot be decrypted, sets the record's metadata to their default values, and resets the credit card
     * engine so any existing server records can overwrite the locally scrubbed records.
     * 
     * NB: This function was created to unblock iOS credit card users who are unable to sync records and should not be used
     * outside of this use case.
     * @param {string} localEncryptionKey
     * @returns {Promise<CreditCardsDeletionMetrics>}}
     */
    async scrubUndecryptableCreditCardDataForRemoteReplacement(
        localEncryptionKey) {
      throw Error("scrubUndecryptableCreditCardDataForRemoteReplacement not implemented");
    }
    /**
     * touchAddress
     * @param {string} guid
     */
    async touchAddress(
        guid) {
      throw Error("touchAddress not implemented");
    }
    /**
     * touchCreditCard
     * @param {string} guid
     */
    async touchCreditCard(
        guid) {
      throw Error("touchCreditCard not implemented");
    }
    /**
     * touchPassport
     * @param {string} guid
     */
    async touchPassport(
        guid) {
      throw Error("touchPassport not implemented");
    }
    /**
     * updateAddress
     * @param {string} guid
     * @param {UpdatableAddressFields} a
     */
    async updateAddress(
        guid, 
        a) {
      throw Error("updateAddress not implemented");
    }
    /**
     * updateCreditCard
     * @param {string} guid
     * @param {UpdatableCreditCardFields} cc
     */
    async updateCreditCard(
        guid, 
        cc) {
      throw Error("updateCreditCard not implemented");
    }
    /**
     * updatePassport
     * @param {string} guid
     * @param {UpdatablePassportFields} p
     */
    async updatePassport(
        guid, 
        p) {
      throw Error("updatePassport not implemented");
    }

}

/**
 * Store
 */
export class Store extends StoreInterface {
    // Use `init` to instantiate this class.
    // DO NOT USE THIS CONSTRUCTOR DIRECTLY
    constructor(opts) {
        super();
        if (!Object.prototype.hasOwnProperty.call(opts, constructUniffiObject)) {
            throw new UniFFIError("Attempting to construct an int using the JavaScript constructor directly" +
            "Please use a UDL defined constructor, or the init function for the primary constructor")
        }
        if (!(opts[constructUniffiObject] instanceof UniFFIPointer)) {
            throw new UniFFIError("Attempting to create a UniFFI object with a pointer that is not an instance of UniFFIPointer")
        }
        this[uniffiObjectPtr] = opts[constructUniffiObject];
    }
    /**
     * init
     * @param {string} dbpath
     * @returns {Promise<Store>}}
     */
    static async init(
        dbpath) {
       
        FfiConverterString.checkType(dbpath);
        const result = await UniFFIScaffolding.callAsyncWrapper(
            18, // uniffi_autofill_fn_constructor_store_new
            FfiConverterString.lower(dbpath),
        )
        return handleRustResult(
            result,
            FfiConverterTypeStore.lift.bind(FfiConverterTypeStore),
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * addAddress
     * @param {UpdatableAddressFields} a
     * @returns {Promise<Address>}}
     */
    async addAddress(
        a) {
       
        FfiConverterTypeUpdatableAddressFields.checkType(a);
        const result = await UniFFIScaffolding.callAsyncWrapper(
            19, // uniffi_autofill_fn_method_store_add_address
            FfiConverterTypeStore.lowerReceiver(this),
            FfiConverterTypeUpdatableAddressFields.lower(a),
        )
        return handleRustResult(
            result,
            FfiConverterTypeAddress.lift.bind(FfiConverterTypeAddress),
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * addCreditCard
     * @param {UpdatableCreditCardFields} cc
     * @returns {Promise<CreditCard>}}
     */
    async addCreditCard(
        cc) {
       
        FfiConverterTypeUpdatableCreditCardFields.checkType(cc);
        const result = await UniFFIScaffolding.callAsyncWrapper(
            20, // uniffi_autofill_fn_method_store_add_credit_card
            FfiConverterTypeStore.lowerReceiver(this),
            FfiConverterTypeUpdatableCreditCardFields.lower(cc),
        )
        return handleRustResult(
            result,
            FfiConverterTypeCreditCard.lift.bind(FfiConverterTypeCreditCard),
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * addPassport
     * @param {UpdatablePassportFields} p
     * @returns {Promise<Passport>}}
     */
    async addPassport(
        p) {
       
        FfiConverterTypeUpdatablePassportFields.checkType(p);
        const result = await UniFFIScaffolding.callAsyncWrapper(
            21, // uniffi_autofill_fn_method_store_add_passport
            FfiConverterTypeStore.lowerReceiver(this),
            FfiConverterTypeUpdatablePassportFields.lower(p),
        )
        return handleRustResult(
            result,
            FfiConverterTypePassport.lift.bind(FfiConverterTypePassport),
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * countAllAddresses
     * @returns {Promise<number>}}
     */
    async countAllAddresses() {
       
        const result = await UniFFIScaffolding.callAsyncWrapper(
            22, // uniffi_autofill_fn_method_store_count_all_addresses
            FfiConverterTypeStore.lowerReceiver(this),
        )
        return handleRustResult(
            result,
            FfiConverterInt64.lift.bind(FfiConverterInt64),
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * countAllCreditCards
     * @returns {Promise<number>}}
     */
    async countAllCreditCards() {
       
        const result = await UniFFIScaffolding.callAsyncWrapper(
            23, // uniffi_autofill_fn_method_store_count_all_credit_cards
            FfiConverterTypeStore.lowerReceiver(this),
        )
        return handleRustResult(
            result,
            FfiConverterInt64.lift.bind(FfiConverterInt64),
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * countAllPassports
     * @returns {Promise<number>}}
     */
    async countAllPassports() {
       
        const result = await UniFFIScaffolding.callAsyncWrapper(
            24, // uniffi_autofill_fn_method_store_count_all_passports
            FfiConverterTypeStore.lowerReceiver(this),
        )
        return handleRustResult(
            result,
            FfiConverterInt64.lift.bind(FfiConverterInt64),
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * deleteAddress
     * @param {string} guid
     * @returns {Promise<boolean>}}
     */
    async deleteAddress(
        guid) {
       
        FfiConverterString.checkType(guid);
        const result = await UniFFIScaffolding.callAsyncWrapper(
            25, // uniffi_autofill_fn_method_store_delete_address
            FfiConverterTypeStore.lowerReceiver(this),
            FfiConverterString.lower(guid),
        )
        return handleRustResult(
            result,
            FfiConverterBoolean.lift.bind(FfiConverterBoolean),
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * deleteCreditCard
     * @param {string} guid
     * @returns {Promise<boolean>}}
     */
    async deleteCreditCard(
        guid) {
       
        FfiConverterString.checkType(guid);
        const result = await UniFFIScaffolding.callAsyncWrapper(
            26, // uniffi_autofill_fn_method_store_delete_credit_card
            FfiConverterTypeStore.lowerReceiver(this),
            FfiConverterString.lower(guid),
        )
        return handleRustResult(
            result,
            FfiConverterBoolean.lift.bind(FfiConverterBoolean),
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * deletePassport
     * @param {string} guid
     * @returns {Promise<boolean>}}
     */
    async deletePassport(
        guid) {
       
        FfiConverterString.checkType(guid);
        const result = await UniFFIScaffolding.callAsyncWrapper(
            27, // uniffi_autofill_fn_method_store_delete_passport
            FfiConverterTypeStore.lowerReceiver(this),
            FfiConverterString.lower(guid),
        )
        return handleRustResult(
            result,
            FfiConverterBoolean.lift.bind(FfiConverterBoolean),
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * getAddress
     * @param {string} guid
     * @returns {Promise<Address>}}
     */
    async getAddress(
        guid) {
       
        FfiConverterString.checkType(guid);
        const result = await UniFFIScaffolding.callAsyncWrapper(
            28, // uniffi_autofill_fn_method_store_get_address
            FfiConverterTypeStore.lowerReceiver(this),
            FfiConverterString.lower(guid),
        )
        return handleRustResult(
            result,
            FfiConverterTypeAddress.lift.bind(FfiConverterTypeAddress),
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * getAllAddresses
     * @returns {Promise<Array.<Address>>}}
     */
    async getAllAddresses() {
       
        const result = await UniFFIScaffolding.callAsyncWrapper(
            29, // uniffi_autofill_fn_method_store_get_all_addresses
            FfiConverterTypeStore.lowerReceiver(this),
        )
        return handleRustResult(
            result,
            FfiConverterSequenceTypeAddress.lift.bind(FfiConverterSequenceTypeAddress),
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * getAllCreditCards
     * @returns {Promise<Array.<CreditCard>>}}
     */
    async getAllCreditCards() {
       
        const result = await UniFFIScaffolding.callAsyncWrapper(
            30, // uniffi_autofill_fn_method_store_get_all_credit_cards
            FfiConverterTypeStore.lowerReceiver(this),
        )
        return handleRustResult(
            result,
            FfiConverterSequenceTypeCreditCard.lift.bind(FfiConverterSequenceTypeCreditCard),
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * getAllPassports
     * @returns {Promise<Array.<Passport>>}}
     */
    async getAllPassports() {
       
        const result = await UniFFIScaffolding.callAsyncWrapper(
            31, // uniffi_autofill_fn_method_store_get_all_passports
            FfiConverterTypeStore.lowerReceiver(this),
        )
        return handleRustResult(
            result,
            FfiConverterSequenceTypePassport.lift.bind(FfiConverterSequenceTypePassport),
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * getCreditCard
     * @param {string} guid
     * @returns {Promise<CreditCard>}}
     */
    async getCreditCard(
        guid) {
       
        FfiConverterString.checkType(guid);
        const result = await UniFFIScaffolding.callAsyncWrapper(
            32, // uniffi_autofill_fn_method_store_get_credit_card
            FfiConverterTypeStore.lowerReceiver(this),
            FfiConverterString.lower(guid),
        )
        return handleRustResult(
            result,
            FfiConverterTypeCreditCard.lift.bind(FfiConverterTypeCreditCard),
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * getPassport
     * @param {string} guid
     * @returns {Promise<Passport>}}
     */
    async getPassport(
        guid) {
       
        FfiConverterString.checkType(guid);
        const result = await UniFFIScaffolding.callAsyncWrapper(
            33, // uniffi_autofill_fn_method_store_get_passport
            FfiConverterTypeStore.lowerReceiver(this),
            FfiConverterString.lower(guid),
        )
        return handleRustResult(
            result,
            FfiConverterTypePassport.lift.bind(FfiConverterTypePassport),
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * registerWithSyncManager
     */
    async registerWithSyncManager() {
       
        const result = await UniFFIScaffolding.callAsyncWrapper(
            34, // uniffi_autofill_fn_method_store_register_with_sync_manager
            FfiConverterTypeStore.lowerReceiver(this),
        )
        return handleRustResult(
            result,
            (result) => undefined,
            null,
        )
    }

    /**
     * Run maintenance on the DB
     * 
     * This is intended to be run during idle time and will take steps / to clean up / shrink the
     * database.
     */
    async runMaintenance() {
       
        const result = await UniFFIScaffolding.callAsyncWrapper(
            35, // uniffi_autofill_fn_method_store_run_maintenance
            FfiConverterTypeStore.lowerReceiver(this),
        )
        return handleRustResult(
            result,
            (result) => undefined,
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * scrubEncryptedData
     */
    async scrubEncryptedData() {
       
        const result = await UniFFIScaffolding.callAsyncWrapper(
            36, // uniffi_autofill_fn_method_store_scrub_encrypted_data
            FfiConverterTypeStore.lowerReceiver(this),
        )
        return handleRustResult(
            result,
            (result) => undefined,
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * The `scrub_undecryptable_credit_card_data_for_remote_replacement` function locally scrubs credit cards
     * that cannot be decrypted, sets the record's metadata to their default values, and resets the credit card
     * engine so any existing server records can overwrite the locally scrubbed records.
     * 
     * NB: This function was created to unblock iOS credit card users who are unable to sync records and should not be used
     * outside of this use case.
     * @param {string} localEncryptionKey
     * @returns {Promise<CreditCardsDeletionMetrics>}}
     */
    async scrubUndecryptableCreditCardDataForRemoteReplacement(
        localEncryptionKey) {
       
        FfiConverterString.checkType(localEncryptionKey);
        const result = await UniFFIScaffolding.callAsyncWrapper(
            37, // uniffi_autofill_fn_method_store_scrub_undecryptable_credit_card_data_for_remote_replacement
            FfiConverterTypeStore.lowerReceiver(this),
            FfiConverterString.lower(localEncryptionKey),
        )
        return handleRustResult(
            result,
            FfiConverterTypeCreditCardsDeletionMetrics.lift.bind(FfiConverterTypeCreditCardsDeletionMetrics),
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * touchAddress
     * @param {string} guid
     */
    async touchAddress(
        guid) {
       
        FfiConverterString.checkType(guid);
        const result = await UniFFIScaffolding.callAsyncWrapper(
            38, // uniffi_autofill_fn_method_store_touch_address
            FfiConverterTypeStore.lowerReceiver(this),
            FfiConverterString.lower(guid),
        )
        return handleRustResult(
            result,
            (result) => undefined,
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * touchCreditCard
     * @param {string} guid
     */
    async touchCreditCard(
        guid) {
       
        FfiConverterString.checkType(guid);
        const result = await UniFFIScaffolding.callAsyncWrapper(
            39, // uniffi_autofill_fn_method_store_touch_credit_card
            FfiConverterTypeStore.lowerReceiver(this),
            FfiConverterString.lower(guid),
        )
        return handleRustResult(
            result,
            (result) => undefined,
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * touchPassport
     * @param {string} guid
     */
    async touchPassport(
        guid) {
       
        FfiConverterString.checkType(guid);
        const result = await UniFFIScaffolding.callAsyncWrapper(
            40, // uniffi_autofill_fn_method_store_touch_passport
            FfiConverterTypeStore.lowerReceiver(this),
            FfiConverterString.lower(guid),
        )
        return handleRustResult(
            result,
            (result) => undefined,
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * updateAddress
     * @param {string} guid
     * @param {UpdatableAddressFields} a
     */
    async updateAddress(
        guid, 
        a) {
       
        FfiConverterString.checkType(guid);
        FfiConverterTypeUpdatableAddressFields.checkType(a);
        const result = await UniFFIScaffolding.callAsyncWrapper(
            41, // uniffi_autofill_fn_method_store_update_address
            FfiConverterTypeStore.lowerReceiver(this),
            FfiConverterString.lower(guid),
            FfiConverterTypeUpdatableAddressFields.lower(a),
        )
        return handleRustResult(
            result,
            (result) => undefined,
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * updateCreditCard
     * @param {string} guid
     * @param {UpdatableCreditCardFields} cc
     */
    async updateCreditCard(
        guid, 
        cc) {
       
        FfiConverterString.checkType(guid);
        FfiConverterTypeUpdatableCreditCardFields.checkType(cc);
        const result = await UniFFIScaffolding.callAsyncWrapper(
            42, // uniffi_autofill_fn_method_store_update_credit_card
            FfiConverterTypeStore.lowerReceiver(this),
            FfiConverterString.lower(guid),
            FfiConverterTypeUpdatableCreditCardFields.lower(cc),
        )
        return handleRustResult(
            result,
            (result) => undefined,
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

    /**
     * updatePassport
     * @param {string} guid
     * @param {UpdatablePassportFields} p
     */
    async updatePassport(
        guid, 
        p) {
       
        FfiConverterString.checkType(guid);
        FfiConverterTypeUpdatablePassportFields.checkType(p);
        const result = await UniFFIScaffolding.callAsyncWrapper(
            43, // uniffi_autofill_fn_method_store_update_passport
            FfiConverterTypeStore.lowerReceiver(this),
            FfiConverterString.lower(guid),
            FfiConverterTypeUpdatablePassportFields.lower(p),
        )
        return handleRustResult(
            result,
            (result) => undefined,
            FfiConverterTypeAutofillApiError.lift.bind(FfiConverterTypeAutofillApiError),
        )
    }

}

// Export the FFIConverter object to make external types work.
export class FfiConverterTypeStore extends FfiConverter {
    static lift(value) {
        const opts = {};
        opts[constructUniffiObject] = value;
        return new Store(opts);
    }

    static lower(value) {
        const ptr = value[uniffiObjectPtr];
        if (!(ptr instanceof UniFFIPointer)) {
            throw new UniFFITypeError("Object is not a 'Store' instance");
        }
        return ptr;
    }

    static lowerReceiver(value) {
        // This works exactly the same as lower for non-trait interfaces
        return this.lower(value);
    }

    static read(dataStream) {
        return this.lift(dataStream.readPointer(4));
    }

    static write(dataStream, value) {
        dataStream.writePointer(4, this.lower(value));
    }

    static computeSize(value) {
        return 8;
    }
}



